# Regras vazias por enquanto -- minifyEnabled fica desligado na release ate
# o app sair da Fase 1 (ver build.gradle.kts). Quando ligar, adicionar regras
# de keep para os modelos kotlinx.serialization (data/model/*.kt).
